# План рефакторинга MZSA GEM

> Документ создан: 2024-12-04  
> Статус: **В процессе**

## 📊 Общее состояние проекта

### ✅ Уже выполнено (Phase 0 — Декабрь 2024)
| Задача | Описание | Файлы |
|--------|----------|-------|
| DATA_SOURCE → env | Перенос константы в `VITE_DATA_SOURCE` | `services/api/index.ts` |
| Секреты | Удалён hardcoded Supabase ключ | `supabaseClient.ts` |
| Debug логи | Удалены ~15 console.log из продакшн-кода | 7 файлов |
| ESLint no-console | Правило warn для новых console.log | `eslint.config.js` |
| Dockerfile | Удалён дубликат, добавлен healthcheck | `Dockerfile` |
| .env.example | Обновлён с документацией | `frontend/.env.example` |

---

## 🚀 Phase 1: Инфраструктура (Высокий приоритет)

### 1.1 Docker оптимизация
- [ ] **`.dockerignore`** — исключить node_modules, .git, тесты
- [ ] **`docker-compose.yml`** — локальная разработка с hot-reload

### 1.2 CI/CD Pipeline
- [ ] **GitHub Actions** — автоматическая сборка и деплой
  - Триггер: push в `main`
  - Шаги: lint → test → build → deploy (Railway)
  - Кеширование node_modules

### 1.3 Безопасность
- [ ] **Dependabot** — автообновление зависимостей
- [ ] **Security audit** — проверка npm audit в CI

---

## 🧹 Phase 2: Качество кода (Средний приоритет)

### 2.1 Тестирование
- [ ] **Unit-тесты** для утилит (`features/trailers`, `utils/`)
- [ ] **E2E тесты** (Playwright) — сценарий "Каталог → Корзина → Заказ"
- [ ] Настроить coverage report в CI

### 2.2 Code Quality
- [ ] Husky + lint-staged для pre-commit хуков
- [ ] Prettier для единообразного форматирования
- [ ] Stricter TypeScript (`strict: true`, `noImplicitAny: true`)

---

## 🏗️ Phase 3: Архитектурные улучшения (Низкий приоритет)

### 3.1 Backend
- [ ] Удалить устаревший `backend/` или пометить как deprecated
- [ ] Рассмотреть Supabase Edge Functions для бизнес-логики

### 3.2 Производительность
- [ ] Bundle analyzer для оптимизации чанков
- [ ] Image optimization pipeline (WebP, AVIF)
- [ ] Service Worker для offline-режима

### 3.3 Мониторинг
- [ ] Sentry для отслеживания ошибок
- [ ] Lighthouse CI для мониторинга производительности

---

## 📋 Чек-лист для начала рефакторинга

1. ✅ Создать план (этот документ)
2. [ ] Добавить `.dockerignore`
3. [ ] Создать GitHub Actions workflow
4. [ ] Добавить Dependabot
5. [ ] Обновить документацию (BACKLOG.md, copilot-instructions.md)

---

## 🔧 Команды для проверки

```bash
# Сборка
cd frontend && npm run build

# Lint
cd frontend && npm run lint

# Тесты
cd frontend && npm run test

# Docker build
docker build -t mzsa-gem --build-arg VITE_DATA_SOURCE=local .
```

---

## 📝 Логирование изменений

### 2024-12-04
- [x] Phase 0: Базовый DevOps-рефакторинг
- [x] Удалены debug console.log
- [x] DATA_SOURCE вынесен в env
- [x] Добавлен healthcheck в Docker
- [ ] Начат Phase 1

---

## 🔗 Связанные документы
- [BACKLOG.md](./BACKLOG.md) — общий бэклог проекта
- [ARCHITECTURE.md](./ARCHITECTURE.md) — архитектура
- [DATA_MODELS.md](./DATA_MODELS.md) — модели данных
