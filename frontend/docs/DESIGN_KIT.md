# Дизайн-кит MZSA GEM

> Единый источник истины для дизайн-токенов, компонентов и UI-паттернов.

---

## 1. Цветовая палитра

### Основные цвета бренда

| Название | Tailwind | HEX | Использование |
|----------|----------|-----|---------------|
| **Brand Blue** | `blue-800` | `#1e40af` | Шапка, основной текст, активные элементы |
| **Brand Light Blue** | `blue-600` | `#2563eb` | Кнопки Primary, ссылки, фокус |
| **Brand Orange** | `orange-500` | `#f97316` | Кнопки Secondary, акценты, CTA |
| **Orange Hover** | `orange-600` | `#ea580c` | Hover-состояние orange-кнопок |

> 💡 В `tailwind.config.js` определены алиасы `brand.blue`, `brand.lightBlue`, `brand.orange`, но пока не используются повсеместно.

### Системные цвета

| Роль | Light Mode | Dark Mode |
|------|------------|-----------|
| Фон страницы | `bg-gray-50` | `bg-gray-900` |
| Фон карточки | `bg-white` | `bg-gray-800` |
| Бордер карточки | `border-gray-100` | `border-gray-700` |
| Основной текст | `text-gray-900` | `text-gray-100` |
| Вторичный текст | `text-gray-600` | `text-gray-400` |
| Приглушённый текст | `text-gray-500` | `text-gray-500` |

### Статусы

| Статус | Фон | Текст | Иконка |
|--------|-----|-------|--------|
| Успех | `bg-green-100` | `text-green-800` | ✓ Check |
| Предупреждение | `bg-yellow-100` | `text-yellow-800` | ⚠ AlertTriangle |
| Ошибка | `bg-red-100` | `text-red-800` | ✕ X |
| Информация | `bg-blue-100` | `text-blue-800` | ℹ Info |

---

## 2. Типографика

Шрифт: **System Stack** (Tailwind default — Inter, system-ui, sans-serif)

### Заголовки

| Элемент | Классы | Пример |
|---------|--------|--------|
| H1 (Hero) | `text-4xl md:text-6xl font-bold` | Заголовок главной страницы |
| H2 (Section) | `text-2xl md:text-3xl font-bold` | Заголовки секций |
| H3 (Card title) | `text-lg font-bold text-gray-900` | Название прицепа в карточке |
| H4 (Subsection) | `text-base font-semibold` | Подзаголовки |

### Текст

| Роль | Классы |
|------|--------|
| Body | `text-base text-gray-700 dark:text-gray-300` |
| Small | `text-sm text-gray-600 dark:text-gray-400` |
| Caption | `text-xs text-gray-500` |
| Price | `text-2xl font-bold text-gray-900` |
| Old Price | `text-lg text-gray-400 line-through` |

---

## 3. Компоненты UI (`src/components/ui/`)

### Button

**Файл:** `Button.tsx`

| Вариант | Классы | Использование |
|---------|--------|---------------|
| `primary` | `bg-blue-600 text-white hover:bg-blue-700` | Основные действия |
| `secondary` | `bg-orange-600 text-white hover:bg-orange-600` | CTA, заказ |
| `outline` | `border-2 border-blue-600 text-blue-600 hover:bg-blue-50` | Вторичные действия |
| `ghost` | `text-gray-600 hover:bg-gray-100` | Иконки, навигация |
| `danger` | `bg-red-600 text-white hover:bg-red-700` | Удаление |
| `success` | `bg-green-600 text-white hover:bg-green-700` | Подтверждение |

**Размеры:**
- `sm`: `px-3 py-1.5 text-sm`
- `md`: `px-4 py-2 text-base` (default)
- `lg`: `px-8 py-3 text-lg`

### Card

**Файл:** `Card.tsx`

```tsx
<Card padding="md" hover>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
  {/* content */}
  <CardFooter>
    <CardActions>...</CardActions>
  </CardFooter>
</Card>
```

**Базовые стили:**
- `bg-white rounded-xl shadow-md border border-gray-100`
- `hover:shadow-lg hover:border-gray-200 transition-all duration-300`

### Input

**Файл:** `Input.tsx`

- Стили: `border border-gray-300 rounded-lg py-2.5`
- Focus: `focus:ring-2 focus:ring-blue-500 focus:border-blue-500`
- Error: `border-red-500 focus:ring-red-500`

### StatusBadge

**Файл:** `StatusBadge.tsx`

Использует утилиты из `utils/orderStatus.ts` для маппинга статусов на стили.

---

## 4. Скругления (Border Radius)

| Элемент | Класс | Значение |
|---------|-------|----------|
| Кнопки | `rounded-lg` | 8px |
| Карточки | `rounded-xl` | 12px |
| Бейджи | `rounded-full` | 9999px |
| Модалки | `rounded-2xl` | 16px |
| Инпуты | `rounded-lg` | 8px |
| Иконки в кругах | `rounded-lg` | 8px |

---

## 5. Тени (Shadows)

| Использование | Класс |
|---------------|-------|
| Карточки (default) | `shadow-sm` |
| Карточки (hover) | `shadow-md` → `shadow-lg` |
| Модалки | `shadow-xl` |
| Кнопки Primary | `shadow-md` |
| CTA кнопки | `shadow-lg shadow-orange-500/30` |

---

## 6. Отступы и сетка

### Container
```css
.container.mx-auto.px-4 /* max-width: 1280px */
```

### Сетка карточек
```html
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
```

### Отступы секций
- Между секциями: `py-12 md:py-16`
- Padding карточек: `p-4`, `p-6`, `p-8`

---

## 7. Анимации (`index.css`)

| Класс | Эффект | Длительность |
|-------|--------|--------------|
| `animate-fadeIn` | Плавное появление | 0.3s |
| `animate-fadeInUp` | Появление снизу | 0.4s |
| `animate-scaleIn` | Масштабирование | 0.3s |
| `animate-slideInRight` | Слайд справа | 0.3s |
| `animate-bounce-small` | Лёгкий bounce | 2s (infinite) |

### Stagger-анимации для списков
```html
<div class="stagger-item" style="animation-delay: 100ms">...</div>
```

---

## 8. Иконки

**Библиотека:** Lucide React (`lucide-react`)

### Часто используемые

| Иконка | Компонент | Контекст |
|--------|-----------|----------|
| 🔍 | `Search` | Поиск |
| 👤 | `User` | Профиль |
| 🛒 | `ShoppingCart` | Корзина |
| ☎️ | `Phone` | Контакты |
| 📍 | `MapPin` | Адрес |
| ⭐ | `Star` | Рейтинг |
| ❤️ | `Heart` | Избранное |
| ✓ | `Check` | Подтверждение |
| ☀️ | `Sun` | Светлая тема |
| 🌙 | `Moon` | Тёмная тема |
| 🚚 | `Truck` | Доставка |
| 🔥 | `Flame` | Популярное |
| ✨ | `Sparkles` | Новинка |

**Размеры:** `size={16}` (sm), `size={20}` (md), `size={24}` (lg)

---

## 9. Dark Mode

Включается через класс `dark` на `<html>`.

### Паттерн
```tsx
className="bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100"
```

### Ключевые переходы

| Light | Dark |
|-------|------|
| `bg-gray-50` | `bg-gray-900` |
| `bg-white` | `bg-gray-800` |
| `border-gray-100` | `border-gray-700` |
| `text-gray-900` | `text-gray-100` |
| `shadow-sm` | `shadow-gray-900/50` |

---

## 10. Breakpoints (из Tailwind)

| Ключ | Ширина | Устройства |
|------|--------|------------|
| `sm` | 640px | Телефоны (landscape) |
| `md` | 768px | Планшеты |
| `lg` | 1024px | Ноутбуки |
| `xl` | 1280px | Десктопы |
| `2xl` | 1536px | Широкие экраны |

**Hook:** `useBreakpoint()` из `hooks/useBreakpoint.ts`

---

## 11. Паттерны компонентов

### Карточка прицепа (TrailerCard)
```
┌─────────────────────────────┐
│ [Image]              [Badge]│
│                             │
│ Модель                      │
│ Название                    │
│                             │
│ • Размеры                   │
│ • Оси • Тормоз              │
│ • Грузоподъёмность          │
│                             │
│ [Цена]         [Кнопка CTA] │
└─────────────────────────────┘
```

### Модальное окно
```
┌─ rounded-2xl shadow-xl ─────┐
│ Header              [Close] │
├─────────────────────────────┤
│                             │
│         Content             │
│                             │
├─────────────────────────────┤
│ Footer          [Actions]   │
└─────────────────────────────┘
```

---

## 12. Чеклист для новых компонентов

- [ ] Поддержка Dark Mode (`dark:` классы)
- [ ] Адаптивность (mobile-first)
- [ ] Состояния: default, hover, focus, disabled
- [ ] Использование токенов из этого документа
- [ ] Анимации переходов (`transition-*`)
- [ ] Accessibility (aria-labels, keyboard navigation)

---

## 13. Состояние переиспользования компонентов

### ✅ Используются
| Компонент | Где используется |
|-----------|------------------|
| `StatusBadge` | `Profile.tsx`, `TrackOrder.tsx` |
| `Card` | `About.tsx`, `Policy.tsx`, `Contacts.tsx`, `Delivery.tsx` |

### ⚠️ Требуют рефакторинга
| Компонент | Inline-дублирования | Приоритет |
|-----------|---------------------|-----------|
| `Button` | 7+ мест (Home, Profile, Configurator, TrailerCard) | Высокий |
| `Card` | ~10 мест (Warranty, TrackOrder, Profile) | Средний |
| `Input` | Не проверено | Низкий |

### Рекомендации по рефакторингу

**Button** — заменить:
```tsx
// ❌ Было
<button className="bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-xl...">

// ✅ Стало  
<Button variant="primary" size="lg">
```

**Card** — заменить:
```tsx
// ❌ Было
<div className="bg-white rounded-xl shadow-sm p-8">

// ✅ Стало
<Card padding="lg">
```

---

## Ссылки

- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [Lucide Icons](https://lucide.dev/icons)
- [Responsive Guidelines](./responsive-guidelines.md)
